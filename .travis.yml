language: c
compiler:
- gcc
- clang
addons:
  apt:
    packages:
    - autoconf
    - autoconf-archive
    - automake
    - elinks
    - gperf
    - libxml2-utils
    - python-docutils
before_script:
- private/autogen
script:
- ./configure
- make -B
- xmllint --noout --valid doc/*.xml
- private/check-rst

# vim:ts=2 sts=2 sw=2 et
